{
  "name": "bet62aposta",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"vite\" \"nodemon server/src/index.ts\"",
    "build": "tsc -p tsconfig.server.json && vite build",
    "start": "node server/dist/index.js",
    "start:server": "node server/dist/index.js",
    "db:migrate": "supabase migration up"
  }
}