{
  "name": "bet62aposta",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"vite\" \"node server/index.js\"",
    "build": "vite build",
    "db:migrate": "node migrations/migrate.js",
    "clean": "rm -rf node_modules dist .env"
  },
  "dependencies": {
    "hono": "latest",
    "lucia": "latest",
    "zod": "latest"
  }
}